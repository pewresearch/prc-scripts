/**
 * Shipped default chart font stack for SVG/canvas chart internals.
 *
 * Installations override via per-role fontFamily in the active chart theme
 * (theme.config) and block attribute defaults. PRC sites seed franklin-gothic
 * via chart-theme.json.
 */
export const DEFAULT_FONT_FAMILY =
	"'franklin-gothic-urw', Verdana, Geneva, sans-serif";
